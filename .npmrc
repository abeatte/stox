# Use the public npm registry for this project.
# Stox has only public dependencies, and pinning the registry here means
# `npm install` works from a fresh clone even on machines whose global
# ~/.npmrc points at an authenticated registry (e.g. AWS CodeArtifact),
# whose tokens expire and otherwise cause a 401 Unauthorized on install.
registry=https://registry.npmjs.org/
