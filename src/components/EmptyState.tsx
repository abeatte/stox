/**
 * Displayed when no tickers are configured.
 */
export function EmptyState() {
  return (
    <div role="status" aria-label="Empty state">
      <p>No tickers configured. Add a ticker above.</p>
    </div>
  );
}
